# Root location of the project
PROJECT_PATH=./..

# Restart mode (always, no, on-failure, unless-stopped)
RESTART_POLICY=unless-stopped

# Deploy mode (dev = autoreload)
MODE=dev

# Frontend port
FRONTEND_PORT=3000

# Backend config
BACKEND_PORT=4000
NEO4J_URL=bolt://server-2021.edgeryders.eu:7687
NEO4J_LOGIN=neo4j
NEO4J_PASSWORD=shen4yaya

# Http host port for the application
HOST_HTTP_PORT=80
